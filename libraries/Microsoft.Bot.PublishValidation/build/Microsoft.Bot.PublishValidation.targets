<Project>
  <!-- -->
  <Target Name="BotConfigurationValidation" AfterTargets="CoreBuild" Condition="$(AvoidDefaultPublishValidation) != 'true'">
    <BotConfigCheckerTask ProjectPath="$(MSBuildProjectDirectory)" />
  </Target>
</Project>